---
agent: build
description: Write a commit message for staged files
---

/conventional-commits Write a commit message for staged files. Ask humans to
decide whether you should continue to run `git commit` with that commit message,
using your questions tool to pop up an interactive selection (only with the
`Yes` option, since humans can press `ecs` to stop here if they choose to).
